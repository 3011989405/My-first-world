清除默认样式
清除标签默认的样式，比如：默认的内外边距
/* 清除默认内外边距 */
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        /* 清除有无序列表项目符号 */
        li{
            list-style: none;
        }