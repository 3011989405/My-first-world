显示模式：标签（元素）的显示方式
作用：布局网页的时候，根据标签的显示模式选择合适的标签拜访内容
显示模式：
块级元素：（用display：block）
		独占一行
		宽度默认是父级的100%
		添加宽高属性生效
行内元素：（用display：inline-block）
		一行可以显示多个
		设置宽高属性不生效
		宽高尺寸由内容撑开
行内块元素：（用display：inline）
		一行可以显示多个
		设置宽高属性生效
		宽高尺寸也可以由内容撑开

